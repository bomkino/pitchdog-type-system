# Font source note

The release consumes the user-supplied `FontBlind-Final-2026-08-28-v13.zip`. Font binaries are deliberately not duplicated inside this distributable. Use the local baker or `tools/prepare_fonts.py` with the separate source archive.
