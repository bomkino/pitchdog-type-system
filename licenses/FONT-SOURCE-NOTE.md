# Font source note

The release consumes the user-supplied `FontBlind-Final-2026-08-28-v13.zip`. Font binaries were deliberately excluded from the original standalone-review distributable. The canonical repository and web package contain the governed runtime files; use the local baker or `tools/prepare_fonts.py` with the separate source archive when rebuilding them.
